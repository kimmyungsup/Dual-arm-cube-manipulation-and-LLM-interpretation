# Python 3.10 runtime requirements for Dual-arm cube manipulation.
#
# Notes:
# - NumPy is pinned below 2.x because OpenCV/Open3D/TFLite/RealSense wheels
#   used by the CubeNet scenario path can fail with NumPy 2.x ABI changes.
# - opencv-contrib-python is required instead of opencv-python because the code
#   uses cv2.xphoto.createSimpleWB().
# - pyrealsense2 and tflite-runtime wheels are platform-specific; install on the
#   robot/camera host that supports Intel RealSense and TFLite runtime wheels.
# - TFLite GPU execution also requires a platform-specific delegate shared
#   library (for example libtensorflowlite_gpu_delegate.so) available on the host.

numpy==1.26.4
pandas==2.2.2
scipy==1.11.4
# Must match the pickled assets/classifier/model.knn distance metric names.
scikit-learn==1.3.2

opencv-contrib-python==4.8.1.78
open3d==0.18.0
pyrealsense2>=2.54,<3

# Hand pose estimation (MediaPipe Hands)
# mediapipe wheels bundle required runtime deps (e.g., protobuf/absl).
mediapipe==0.10.14
tflite-runtime==2.14.0
kociemba==1.2.1

# Optional chat/voice-command integrations used by dual_arm_main9.py.
openai>=1.0,<2
sounddevice==0.4.6
soundfile==0.12.1
